{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window. `process:allow-exit` is here rather than in the desktop capability because Android's back button at the root screen is meant to close the app.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default",
    "process:allow-exit"
  ]
}
