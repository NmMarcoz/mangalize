{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Self-updating and relaunching. Desktop only: Android updates through its store and the updater plugin has no implementation there, so naming its permissions in a shared capability breaks the mobile build.",
  "platforms": [
    "macOS",
    "windows",
    "linux"
  ],
  "windows": [
    "main"
  ],
  "permissions": [
    "updater:default",
    "process:allow-restart"
  ]
}
